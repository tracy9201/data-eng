import React from 'react'
import Str from '@components/string/str'
import VisProvider from '@components/vis-provider'

const App = () => {
  return (
    <VisProvider>
      <Str />
    </VisProvider>
  )
}

export default App
